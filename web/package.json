{
  "name": "resopal-web",
  "private": true,
  "type": "module",
  "description": "Browser build of the deck generator: writes each card's art as a URL into the v1.0 deck template, and composes the raw sheet for hand-baking, entirely client-side.",
  "scripts": {
    "build": "esbuild bake.js --bundle --format=esm --outfile=bake.bundle.js && cp node_modules/brotli-wasm/pkg.web/brotli_wasm_bg.wasm .",
    "test:fill": "node test-fill.mjs"
  },
  "dependencies": { "bson": "^7.2.0", "brotli-wasm": "^3.0.1", "jszip": "^3.10.1" },
  "devDependencies": { "esbuild": "^0.25.0" }
}
